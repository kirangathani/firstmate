working: worktree verified, branch fm/eln-live-comments-a1, migration (trigger + realtime.messages policy) written and verified on the local stack
paused: captain pause 2026-09-14; handover at data/eln-live-comments-a1/handover.md; no run
working: resumed; migrate-fresh-check passes (realtime.messages shell added); rebuilding the shared local stack after another lane's unmerged migration broke pnpm test:db, then the db + vitest cases
working: shared local stack was re-shaped by another lane's in-flight 20260914190000_remove_projects (worktree 8), which breaks pnpm test:db on every main-based branch; resetting it to main + my migration to run the suite
working: all gates green on a stack matching this branch (vitest 109 files/1416, test:db all rules hold, migrate-fresh-check OK, ui-sweep 71 checks nothing clipped) and the two-browser acceptance passed 7/7 (create/edit/resolve/anchored-create/re-anchor all land in the other tab in 0.97-1.5s, no reload, half-typed schedule field intact); starting no-mistakes
needs-decision: review gate parked, 3 ask-user findings at severity WARNING (not mine per the brief) - full text + my analysis at data/eln-live-comments-a1/review-findings.md. review-1: REAL cross-tab bug this branch introduces, the local-origin gate misses reconcile()'s second caller so a stale tab overwrites a live tab's anchor - I recommend fix (gate the anchors-keyed effect to thread-SET changes only). review-3: anchor-move UPDATEs fan a full reload to every tab - the reviewer's narrowing would break the brief's own 're-anchored appears in the other tab' acceptance, so I recommend approve + follow-up. review-4: reviewer's premise is factually wrong (pdf-comment-surface.test.tsx on main DOES render CommentLayout with no supabase env, so the guard is load-bearing) but its second half is right (refetch is dead on the no-env path) - I recommend fix that hole and KEEP the guard. review-2 (auto-fix) and review-5 (no-op) are mine and handled. Run 01M2GJTH1EJRDBBJTJ6PJS05TQ is parked, not aborted.
needs-decision: [key=a1-review-r7] fix round applied review-2 + review-4 as decided; re-review raised TWO new findings. review-6 (warning/auto-fix, concurrent refetches have no ordering guard so a slow older snapshot can silently drop a newer comment) is MINE and I will submit it. review-7 (warning/ASK-USER, so yours) - the new failed flag is dropped at the RSC prop boundary, so a router.refresh() whose comments select times out still wipes a live margin; it contradicts wording that was MINE, not a captain decision, so it is a new question not a re-raise. It is new damage from this branch (a failed refresh now destroys accumulated live state, recovered only by the next broadcast, possibly minutes). Remedy is ~15 lines but touches three route files as one-line prop passes. I recommend FIX. Full text and analysis appended to data/eln-live-comments-a1/review-findings.md. Run 01M2GJTH1EJRDBBJTJ6PJS05TQ parked at fix_review, not aborted; this would be fix round 2 of the cap.
resolved: [key=a1-review-r1] captain decisions applied at the review gate - review-1 approve/no-change (phase (b) closes the stale-body window), review-3 approve/no-change (a re-anchor IS an anchor-only UPDATE and must stay live), review-4 fix keeping the env guard, review-2 fixed in the same round; all four recorded via fm-nm-decision.sh and now in the pinned intent
resolved: [key=a1-review-r7] captain decided FIX; sent as fix round 2 with review-6 (refetch ordering guard), both recorded via fm-nm-decision.sh and now in the pinned intent
